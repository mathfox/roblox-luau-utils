local CharacterUtils = {
	getPlayerHumanoid = require(script.getPlayerHumanoid),
	getPlayerHumanoidFast = require(script.getPlayerHumanoidFast),

	getAlivePlayerHumanoid = require(script.getAlivePlayerHumanoid),
	getAlivePlayerHumanoidFast = require(script.getAlivePlayerHumanoidFast),

	getPlayerRootPart = require(script.getPlayerRootPart),
	getPlayerRootPartFast = require(script.getPlayerRootPartFast),

	getAlivePlayerRootPart = require(script.getAlivePlayerRootPart),
	getAlivePlayerRootPartFast = require(script.getAlivePlayerRootPartFast),

	unequipPlayerTools = require(script.unequipPlayerTools),
	unequipPlayerToolsFast = require(script.unequipPlayerToolsFast),

	getPlayerFromCharacterDescendant = require(script.getPlayerFromCharacterDescendant),
	getPlayerFromCharacterDescendantFast = require(script.getPlayerFromCharacterDescendantFast),

	waitForPlayerCharacter = require(script.waitForPlayerCharacter),
	waitForPlayerCharacterFast = require(script.waitForPlayerCharacterFast),
}

return CharacterUtils
